#include <iostream>
using namespace std;

/*
? malloc(): used to dynamically allcoate a single large block of memory with the specified size.
? it returns a pointer of type void which can be cast into a pointer of any form.  
*/

int main()
{
    return 0;
}